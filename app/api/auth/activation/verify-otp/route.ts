import {NextResponse} from 'next/server';import {jsonError,serverError} from '@/lib/api';import {verifyActivation} from '@/lib/activation';import {rateLimit} from '@/lib/rate-limit';export async function POST(r:Request){try{const b=await r.json() as {email?:string;otp?:string};if(!b.email||!b.otp||!await rateLimit(`verify:${b.email}`,10,600))return jsonError('Permintaan tidak valid.',400);const ticket=await verifyActivation(b.email,b.otp);return ticket?NextResponse.json({ticket}):jsonError('OTP tidak valid atau kedaluwarsa.',400)}catch(e){return serverError(e)}}

'use client';import {useState} from 'react';import {ClientShell} from '../client-shell';import {useRouter} from 'next/navigation';
export default function ChangePassword(){const [currentPassword,setCurrent]=useState(''),[newPassword,setNew]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();if(newPassword!==confirm){setError('Konfirmasi password tidak sama.');return}setLoading(true);const response=await fetch('/api/auth/change-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({currentPassword,newPassword})});const data=await response.json();if(!response.ok){setError(data.error);setLoading(false);return}router.replace('/');router.refresh()}return <ClientShell><div className="card mx-auto max-w-lg p-7"><h1 className="text-xl font-bold">Change Password</h1><p className="mt-2 text-xs text-slate-400">Gunakan minimal 8 karakter untuk password baru.</p><form onSubmit={submit} className="mt-6 space-y-4">{[['Current Password',currentPassword,setCurrent],['New Password',newPassword,setNew],['Confirm New Password',confirm,setConfirm]].map(([label,value,setter])=><label className="block text-xs font-semibold" key={label as string}>{label as string}<input type="password" required className="field mt-2 w-full" value={value as string} onChange={e=>(setter as (v:string)=>void)(e.target.value)}/></label>)}{error&&<p className="text-xs text-red-400">{error}</p>}<button disabled={loading} className="btn btn-primary">{loading?'Menyimpan...':'Ubah Password'}</button></form></div></ClientShell>}

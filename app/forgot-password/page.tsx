'use client';import {FormEvent,useState} from 'react';export default function Page(){const[msg,setMsg]=useState('');async function send(e:FormEvent<HTMLFormElement>){e.preventDefault();const email=new FormData(e.currentTarget).get('email');const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});setMsg((await r.json()).message||'Silakan periksa email Anda.')}return <main className="flex min-h-screen items-center justify-center"><form onSubmit={send} className="card w-full max-w-md space-y-4 p-8"><h1 className="text-2xl font-bold">Forgot Password</h1><input name="email" type="email" required className="field w-full" placeholder="Email"/><button className="btn btn-primary w-full">Kirim Reset Link</button>{msg&&<p>{msg}</p>}</form></main>}

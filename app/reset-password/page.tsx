'use client';import {FormEvent,useState} from 'react';export default function Page({searchParams}:{searchParams:{token?:string}}){const token=searchParams.token,[msg,setMsg]=useState('');async function reset(e:FormEvent<HTMLFormElement>){e.preventDefault();const password=new FormData(e.currentTarget).get('password');const r=await fetch('/api/auth/reset-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json();setMsg(d.message||d.error)}return <main className="flex min-h-screen items-center justify-center"><form onSubmit={reset} className="card w-full max-w-md space-y-4 p-8"><h1 className="text-2xl font-bold">Reset Password</h1><input name="password" type="password" required className="field w-full"/><button className="btn btn-primary w-full">Simpan Password</button>{msg&&<p>{msg}</p>}</form></main>}

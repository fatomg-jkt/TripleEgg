'use client';
import Link from 'next/link';import Image from 'next/image';import {useState} from 'react';import {Eye,EyeOff,Loader2} from 'lucide-react';import {useRouter,useSearchParams} from 'next/navigation';
export function AuthBrand(){return <div className="mb-8 text-center"><Image src="/triple-egg-logo.svg" alt="Triple Egg" width={144} height={80} className="mx-auto h-20 w-36"/><h1 className="mt-4 text-xl font-bold">Financial & Accounting Triple Egg</h1><p className="mt-2 text-xs text-slate-500">Masuk untuk melanjutkan ke workspace Anda</p></div>}
export function LoginForm(){const router=useRouter(),params=useSearchParams();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[remember,setRemember]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password,remember})});const data=await response.json();if(!response.ok)throw new Error(data.error);router.replace(data.user.requirePasswordChange?'/change-password':params.get('next')||'/');router.refresh()}catch(err){setError(err instanceof Error?err.message:'Login gagal.')}finally{setLoading(false)}}return <form onSubmit={submit} className="space-y-4"><label className="block text-xs font-semibold text-slate-300">Email<input required type="email" autoComplete="email" className="field mt-2 w-full" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block text-xs font-semibold text-slate-300">Password<div className="relative mt-2"><input required type={show?'text':'password'} autoComplete="current-password" className="field w-full pr-10" value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" aria-label="Show password" onClick={()=>setShow(!show)} className="absolute right-3 top-2.5 text-slate-500">{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div role="alert" className="rounded-lg border border-red-500/30 bg-red-500/10 p-3 text-xs text-red-300">{error}</div>}<div className="flex items-center justify-between text-xs"><label className="flex items-center gap-2 text-slate-400"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/> Remember Me</label><Link href="/forgot-password" className="text-blue-400 hover:text-blue-300">Forgot Password</Link></div><button disabled={loading} className="btn btn-primary w-full disabled:opacity-60">{loading?<><Loader2 className="animate-spin" size={15}/> Signing in...</>:'Sign In'}</button></form>}
export function ForgotForm(){const [email,setEmail]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const response=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const data=await response.json();setMessage(data.message);setLoading(false)}return <form onSubmit={submit} className="space-y-4"><label className="block text-xs font-semibold">Email<input required type="email" className="field mt-2 w-full" value={email} onChange={e=>setEmail(e.target.value)}/></label>{message&&<p className="rounded-lg bg-blue-500/10 p-3 text-xs text-blue-300">{message}</p>}<button disabled={loading} className="btn btn-primary w-full">{loading?'Mengirim...':'Kirim Instruksi Reset'}</button><Link href="/login" className="block text-center text-xs text-blue-400">Kembali ke Login</Link></form>}

'use client';
import {FormEvent,useState} from 'react';import {useRouter,useSearchParams} from 'next/navigation';
async function safeJson(response:Response){const text=await response.text();if(!text)return null;try{return JSON.parse(text) as {error?:string}}catch{return null}}
export function LoginForm(){const router=useRouter(),params=useSearchParams();const [error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setLoading(true);const form=new FormData(e.currentTarget);try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});const data=await safeJson(response);if(!response.ok){setError(data?.error||'Login gagal. Silakan coba lagi.');return}router.replace(params.get('next')||'/');router.refresh()}catch{setError('Tidak dapat terhubung ke server. Periksa koneksi lalu coba lagi.')}finally{setLoading(false)}}return <form onSubmit={submit} className="w-full max-w-md space-y-5 rounded-2xl border border-slate-700 bg-[#0d1928] p-8 shadow-2xl"><div className="text-center"><img src="/triple-egg-logo.svg" alt="Triple Egg" className="mx-auto mb-5 h-16"/><h1 className="text-2xl font-bold text-white">Masuk ke Triple Egg</h1><p className="mt-2 text-sm text-slate-400">Financial & Accounting Dashboard</p></div>{error&&<div role="alert" className="rounded-lg border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-300">{error}</div>}<label className="block text-sm text-slate-300">Email<input name="email" type="email" autoComplete="email" required className="field mt-2 w-full"/></label><label className="block text-sm text-slate-300">Password<input name="password" type="password" autoComplete="current-password" required className="field mt-2 w-full"/></label><button disabled={loading} className="w-full rounded-lg bg-blue-600 py-3 font-semibold text-white hover:bg-blue-500 disabled:opacity-60">{loading?'Memproses…':'Login'}</button></form>}

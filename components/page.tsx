'use client';
import {useState} from 'react';import {Download,FileDown,Plus,Printer,Upload} from 'lucide-react';import {UploadModal} from './upload';import {DataTable,UploadHistory} from './data-table';import {FilterBar} from './dashboard';
export function PageHeader({title,subtitle,onUpload}:{title:string,subtitle:string,onUpload:()=>void}){return <div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-center"><div><div className="label mb-2 text-blue-500">Finance / Overview</div><h1 className="text-2xl font-bold tracking-tight">{title}</h1><p className="mt-1 text-xs text-slate-500">{subtitle}</p></div><div className="flex flex-wrap gap-2"><button onClick={onUpload} className="btn btn-primary"><Upload size={14}/> Upload File</button><button className="btn"><FileDown size={14}/> Download Template</button><button className="btn"><Download size={14}/> Export Excel</button><button className="btn desktop-only"><Printer size={14}/> Print</button></div></div>}
export function ModulePage({title,subtitle,slug}:{title:string,subtitle:string,slug:string}){const [open,setOpen]=useState(false);const [tab,setTab]=useState<'data'|'history'>('data');return <><PageHeader title={title} subtitle={subtitle} onUpload={()=>setOpen(true)}/><div className="mt-6"><FilterBar/></div><div className="mt-6 flex border-b border-[#203047]"><button onClick={()=>setTab('data')} className={`${tab==='data'?'border-blue-500 text-white':'border-transparent text-slate-500'} border-b-2 px-5 py-3 text-xs font-semibold`}>Data</button><button onClick={()=>setTab('history')} className={`${tab==='history'?'border-blue-500 text-white':'border-transparent text-slate-500'} border-b-2 px-5 py-3 text-xs font-semibold`}>Upload History</button></div><div className="mt-4">{tab==='data'?<><div className="mb-4 flex items-center justify-between"><div><h2 className="text-sm font-semibold">Data {title}</h2><p className="mt-1 text-[10px] text-slate-500">Data mock siap dihubungkan dengan API/database.</p></div><button className="btn"><Plus size={14}/> Tambah Data</button></div><DataTable type={slug}/></>:<UploadHistory/>}</div><UploadModal open={open} onOpenChange={setOpen} module={title}/></>}

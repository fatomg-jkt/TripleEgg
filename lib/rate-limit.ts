import {database} from './db';export async function rateLimit(key:string,limit:number,seconds:number){await database().query('CREATE TABLE IF NOT EXISTS auth_rate_limits(key text PRIMARY KEY,count integer NOT NULL,window_start timestamptz NOT NULL)');const r=await database().query(`INSERT INTO auth_rate_limits VALUES($1,1,now()) ON CONFLICT(key) DO UPDATE SET count=CASE WHEN auth_rate_limits.window_start<now()-make_interval(secs=>$2) THEN 1 ELSE auth_rate_limits.count+1 END,window_start=CASE WHEN auth_rate_limits.window_start<now()-make_interval(secs=>$2) THEN now() ELSE auth_rate_limits.window_start END RETURNING count`,[key,seconds]);return r.rows[0].count<=limit}
